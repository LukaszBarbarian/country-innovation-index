name: cv demo1 CI/CD orchestrator

on:
  push:
    branches:
      - main
    paths:
      - 'infra/**'
      - './'
      - '.github/workflows/main.yml'
  workflow_dispatch:

jobs:
  deploy-dev:
    name: "Deploy DEV"
    uses: lukaszBarbarian/country-innovation-index/.github/workflows/deploy-dev.yml@main

  deploy-prod:
    name: "Deploy PROD"
    needs: deploy-dev
    uses: lukaszBarbarian/country-innovation-index/.github/workflows/deploy-prod.yml@main
